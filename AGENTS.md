# AGENTS.md — Sake Tasting Log App 작업 지침

이 파일은 AI 에이전트/코딩 에이전트가 이 프로젝트를 수정할 때 반드시 따라야 할 **절대적 행동 기준**이다.

현재 프로젝트의 아키텍처 및 시스템 기준 문서는 `CONTEXT.md`이다.

---

## 1. 현재 제품 방향

이 프로젝트는 범용 주류 기록 앱이 아닌 **사케(Sake) 전용 테이스팅 로그 앱**이다.

> 모바일 환경에서 사케를 마실 때 빠르게 사진, 기본 정보, 맛의 방향, 다시 마실 의향, 장소 및 동행 정보를 기록하고 클라우드에 안전하게 보관한다.

위스키, 진, 럼, 와인, 한국 전통주 등은 지금 구현하지 않는다. 다만 나중에 확장할 수 있도록 `drink_type` 필드 정도만 남긴다.

---

## 2. 최우선 원칙

1. 사케 테이스팅 경험과 안정적인 클라우드 동기화에 집중한다.
2. 술을 마신 상태에서도 1분 내로 기록할 수 있게 단순해야 한다.
3. 술 이름만 필수다. 나머지는 선택 입력이다.
4. 사진 여러 장을 저장하고 썸네일을 표시할 수 있어야 한다.
5. 커스텀 태그는 추가 후 다음 기록에서도 다시 보여야 한다.
6. UI 순서는 `CONTEXT.md`의 “오늘의 한 잔 기록” 순서를 엄격히 따른다.
7. 확장성 때문에 현재 UX를 복잡하게 만들지 않는다.

---

## 3. 하지 말 것

다음 작업은 사용자가 명시적으로 요청하기 전까지 하지 않는다.

- 범용 flavor ontology 설계
- 위스키/진/럼/와인 전용 화면 구현
- Nose / Palate / Finish 구조 강제
- 별점 시스템 추가
- 복잡한 통계 대시보드 구현
- AI 추천 구현
- OCR 구현
- 라벨 자동 인식 구현
- 공개 공유 기능 구현
- 동행/안주를 별도 테이블로 과도하게 정규화
- 가격/도수/용량을 처음부터 숫자 정규화
- 태그 삭제/병합 기능 구현
- 이미지 순서 변경 기능 구현

필요해 보여도 하지 않는다. 단순하고 확실한 기록 경험이 먼저다.

---

## 4. Mold Native 아키텍처 작업 규칙

1. **단일 진실 공급원 (Single Source of Truth)**:
   * 데이터 모델, 필드 타입, 제약조건, 권한(`permissions`) 수정은 항상 `resources/*.yaml`에서 먼저 수행한다.
2. **생성 코드 직접 수정 금지**:
   * `functions/_shared/generated/mold_app.ts`는 언제든 재생성될 수 있는 산출물이다. 이 파일을 직접 수정하여 비즈니스 로직을 구현하지 않는다.
3. **외부 브릿지 및 어댑터 활용**:
   * 인증 연동, 세션 주입, 특수 헤더 처리 등은 `functions/api/[[path]].ts` 또는 `functions/_shared/auth.ts`와 같은 상위 게이트웨이 레이어에서 처리한다.

---

## 5. 핵심 입력 플로우

사케 기록 생성 화면은 아래 순서를 따른다.

1. 사진들
2. 기본 정보
3. 다시 마실까?
4. 평가
5. 한줄 메모
6. 특성 태그
7. 외부 정보

이 순서를 임의로 바꾸지 않는다.

---

## 6. 필드 정의 및 선택지

### 6.1 기본 정보
- 술 이름: **필수**
- 지역, 양조장, 쌀, 종류, 일본주도, 도수, 용량, 가격: **선택** (문자열 저장)

### 6.2 다시 마실까? (단일 선택)
- 표시값: `별로` / `잘모르겠음` / `다시 마신다`
- 저장값: `no` / `unsure` / `yes`

### 6.3 평가 (버튼형 단일 선택)
- **달콤함 - 드라이함**: `아주 달콤함(1)` ~ `아주 드라이함(5)` (5단계)
- **은은함 - 화려함**: `은은한향(1)` / `보통(2)` / `화려한향(3)`
- **산미**: `산미없음(1)` / `산미보통(2)` / `산미높음(3)`
- **깔끔함 - 감칠맛**: `깔끔함(1)` / `보통(2)` / `감칠맛좋은(3)`

### 6.4 특성 태그
- 그룹: 맛(`taste`), 향(`aroma`), 느낌(`mood`)
- 각 그룹 끝에 `+` 버튼을 배치하며, 추가 시 20자 제한, trim, 중복 체크 후 즉시 자동 선택된다.

### 6.5 외부 정보
- 장소: 선택
- 날짜: 기본값 오늘 (수정 가능)
- 동행: 선택
- 안주: 선택

---

## 7. 검증 규칙

코드 수정 후 반드시 다음 3가지 검증을 실행하여 0 error를 확인한다:

```powershell
npm.cmd run typecheck
npm.cmd run typecheck:functions
npm.cmd run build
```

---

## 8. 최종 판단 기준

기능을 추가하거나 변경할지 애매하면 다음 질문으로 판단한다.

> **이 변경이 모바일에서 사케를 빠르고 즐겁게 기록하고 시음 경험을 쌓는 데 직접 도움이 되는가?**

답이 아니면 지금은 하지 않는다. 이 프로젝트는 작고 단단하게 유지되어야 한다.
